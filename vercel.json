{
  "version": 2,
  "buildCommand": "mv tsconfig.json tsconfig.json.bak && npm run build; BUILD_CODE=$?; mv tsconfig.json.bak tsconfig.json; exit $BUILD_CODE",
  "installCommand": "npm install",
  "framework": "nextjs",
  "outputDirectory": ".next",
  "git": {
    "deploymentEnabled": {
      "main": true,
      "staging": true,
      "develop": true
    }
  },
  "github": {
    "enabled": true
  }
}
